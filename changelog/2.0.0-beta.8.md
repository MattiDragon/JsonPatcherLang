## Core language
* Parser now longer hangs on unclosed blocks

## Documentation tool
* Added new `@optional` doc tag to mark optional properties

## Standard library
* Fixes syntax issues in documentation for `string.substring` and `values.freeze`
* Reimplemented math using reflection (should not affect users at all)
* Added `values.getProperty` and `values.setProperty` for rare use-cases
  * They allow access to properties as though through the `.` operator, but with dynamic property names.
    They should rarely see any use.
* Fixed issue with boolean arguments when calling methods through reflection