* Completed new doc engine and got rid of old one
* Lang server
  * Now uses new docs engine
  * Now has a proper indexing setup
    * This allows easier integration of new features
    * Improves hover and goto 
  * Now has a type checker
* Support conversion of functions into SAM interfaces
* Integrated every CLI tool into a single program
  * Currently has the doc tool and a new AST printer 